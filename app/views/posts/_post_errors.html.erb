<% if @post.errors.any? %>
  <div class="alert">
    <strong>The form contains <%= pluralize(@post.errors.count, "error") %>.</strong>
  </div>

  <% @post.errors.full_messages.each do |message| %>
    <p><%= message %></p>
  <% end %>
<% end %>
